[project]
type = "nodejs"

[phases.install]
command = "npm ci --cache /tmp/npm-cache"

[phases.build]
command = "npm run build"

[phases.start]
command = "npm run start"

[cache]
paths = [
  "node_modules",
  "dist"
]
